<?xml version="1.0" encoding="utf-8"?>
<!-- Ultra-minimal initial layout used for the first render before onUpdate runs.
     Uses only solid color (no gradient/shape drawable), three TextViews.
     This must never fail to inflate. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#6366F1"
    android:padding="12dp"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/widget_account_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sure Finances"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_month_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Loading..."
        android:textColor="#CCFFFFFF"
        android:textSize="10sp"
        android:layout_marginTop="2dp" />

    <TextView
        android:id="@+id/widget_monthly_total"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="4dp" />

</LinearLayout>
